package com.sanan.avatarcore.util.component;

/*import java.util.ArrayList;
import java.util.List;

import net.kyori.adventure.text.Component;
import net.kyori.adventure.text.TextComponent;
import net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer;*/

public class ComponentUtil{
	
	/*public static String getDisplayName(ItemStack item) {
		if (item.getItemMeta().hasDisplayName()) {
			return LegacyComponentSerializer.legacyAmpersand().serialize(item.displayName());
		}
		return "";
	}

	public static List<String> getLore(ItemStack item) {
		List<String> lore = new ArrayList<String>();
		if (item.getItemMeta().hasLore()) {
			item.lore().forEach(line -> lore.add(LegacyComponentSerializer.legacyAmpersand().serialize(line)));
		}
		return lore;
	}
	
	public static String getString(Component message) {
		return LegacyComponentSerializer.legacyAmpersand().serialize(message);
	}
	
	public static TextComponent toComponent(String message) {
		return LegacyComponentSerializer.legacyAmpersand().deserialize(message);
	}*/
	
}
